<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Merged with Xcode's auto-generated Info.plist (GENERATE_INFOPLIST_FILE).
         Holds the URL scheme + the string keys Xcode no longer injects once INFOPLIST_FILE is set. -->
    <key>CFBundleDisplayName</key>
    <string>TargetBridge</string>
    <key>NSHumanReadableCopyright</key>
    <string>TargetBridge - Free &amp; Open Source</string>
    <key>NSScreenCaptureUsageDescription</key>
    <string>TargetBridge requires screen recording permission to capture the virtual display or mirror the desktop to the iMac.</string>
    <key>NSAppleEventsUsageDescription</key>
    <string>TargetBridge uses System Events to run shortcuts selected for Receiver is Master.</string>
    <!-- Capturing any CoreAudio input device counts as microphone access, including
         a loopback device such as BlackHole. Without this key macOS denies the
         capture silently rather than prompting. -->
    <key>NSMicrophoneUsageDescription</key>
    <string>TargetBridge captures a selected audio device so the sound routed to it plays on the receiver Mac.</string>
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>com.targetbridge.sender</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>targetbridge</string>
            </array>
        </dict>
    </array>
</dict>
</plist>
